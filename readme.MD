# 🔥 UFW Manager

Интерактивный Bash-скрипт для управления **UFW (Uncomplicated Firewall)** с удобным меню, валидацией данных и безопасной работой по SSH.

Проект предназначен для администраторов серверов и пользователей Linux, которым нужен простой и надёжный способ управлять firewall.

---

## 🚀 Возможности

- 📋 Просмотр текущих правил UFW
- ➕ Добавление правил:
  - типовые (SSH, HTTP, HTTPS)
  - из файла конфигурации
  - вручную
- ➖ Удаление правил:
  - типовые (HTTP, HTTPS)
  - из конфигурационного файла
  - по номеру (с защитой SSH)
- ✏️ Редактирование `rules.config`
- 🔐 Защита от удаления SSH (22/tcp)
- ♻️ Защита от дубликатов правил
- 🌐 Поддержка:
  - направлений: `IN`, `OUT`, `BOTH`
  - протоколов: `tcp`, `udp`, `both`
- ✅ Валидация портов (1–65535)
- 📄 Автосоздание и автозаполнение `rules.config`

---

## 📦 Требования

- Linux (Ubuntu / Debian / совместимые)
- Bash 4+
- UFW установлен и включён

Проверка:
```bash
ufw status
```

Если UFW не установлен:
```bash
sudo apt install ufw
sudo ufw enable
```

---

## 📥 Установка

### 1. Клонирование репозитория
```bash
git clone https://github.com/USERNAME/ufw-manager.git
cd ufw-manager
```

### 2. Права на выполнение
```bash
chmod +x ufw-manager.sh
```

---

## ▶️ Запуск

⚠️ **Скрипт должен запускаться от root**
```bash
sudo ./ufw-manager.sh
```

При первом запуске:
- автоматически создаётся `rules.config`
- файл заполняется типовыми правилами:
  - SSH (22/tcp)
  - HTTP (80/tcp)
  - HTTPS (443/tcp)

---

## 📄 Формат rules.config

```text
Имя_правила:НАПРАВЛЕНИЕ:ПОРТ:ПРОТОКОЛ
```

### Пример:
```text
SSH:IN:22:tcp
HTTP:IN:80:tcp
HTTPS:IN:443:tcp
DNS:OUT:53:udp
CUSTOM:BOTH:12345:both
```

---

## 🧭 Главное меню

```text
1. Проверка текущих правил
2. Добавление правил
3. Удаление правил
4. Редактирование списка правил
0. Выход
```

---

## ➕ Добавление правил

### Типы добавления:
1. Типовые правила (SSH, HTTP, HTTPS)
2. Из файла `rules.config`
3. Вручную (с валидацией)

### Поддерживаемые параметры:
- Направление: `IN`, `OUT`, `BOTH`
- Протокол: `tcp`, `udp`, `both`
- Порт: 1–65535

---

## ➖ Удаление правил

### Способы:
1. Типовые (HTTP, HTTPS)
2. Из `rules.config`
3. По номеру (`ufw status numbered`)

🔐 **Правило SSH (22/tcp) защищено от удаления**  
Удаление по номеру будет заблокировано автоматически.

---

## ✏️ Редактирование rules.config

- Просмотр с номерами строк
- Добавление правил с проверкой дубликатов
- Удаление по номеру строки

---

## 🛡️ Безопасность

- SSH нельзя удалить ни через меню, ни по номеру
- Дубликаты правил не добавляются
- Некорректные порты и протоколы отклоняются

---

## 🧪 Рекомендации

- Перед массовыми изменениями делайте бэкап:
```bash
cp rules.config rules.config.bak
```
- Всегда проверяйте правила:
```bash
ufw status verbose
```

---

## 📌 Планы по развитию

- dry-run режим
- backup / restore
- systemd unit
- dialog / whiptail интерфейс
- синхронизация UFW ↔ rules.config

---

## 📜 Лицензия

MIT License

---

## 🤝 Автор

Разработано для практического и безопасного администрирования UFW.
Pull requests и идеи приветствуются 🚀
